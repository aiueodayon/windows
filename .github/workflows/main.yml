name: Myrtille Cloudflare Tunnel

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - name: 🧰 Setup PowerShell
        shell: pwsh
        run: |
          Write-Host "PowerShell version:" $PSVersionTable.PSVersion


      - name: 📦 Download latest Myrtille MSI
        shell: pwsh
        run: |
          $release = Invoke-RestMethod "https://api.github.com/repos/cedrozor/myrtille/releases/latest"
          $asset = $release.assets | Where-Object { $_.name -like "*.msi" } | Select-Object -First 1
          if (-not $asset) { Write-Error "❌ Myrtille MSI not found."; exit 1 }
          Write-Host "Found MSI: $($asset.name)"
          Invoke-WebRequest $asset.browser_download_url -OutFile Myrtille.msi
          Write-Host "✅ Downloaded Myrtille.msi"


      - name: 📂 Extract MSI to folder (bypass installer)
        shell: pwsh
        run: |
          mkdir C:\Myrtille
          Start-Process msiexec.exe -ArgumentList '/a', (Resolve-Path Myrtille.msi), '/qn', "TARGETDIR=C:\Myrtille" -Wait
          Write-Host "✅ Myrtille extracted to C:\Myrtille"
    

      - name: 🚀 Start Myrtille Web + Service
        shell: pwsh
        run: |
          cd C:\Myrtille
          Start-Process -FilePath "Myrtille.Services.exe" -NoNewWindow
          Start-Process -FilePath "Myrtille.Web.exe" -NoNewWindow
          Write-Host "✅ Myrtille started on port 80."
      
      
      - name: 🌐 Download Cloudflared
        shell: pwsh
        run: |
          Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe
          Write-Host "✅ Cloudflared downloaded."


      - name: ☁️ Start Cloudflare Tunnel
        shell: pwsh
        env:
          CLOUDFLARED_TOKEN: ${{ secrets.CLOUDFLARED_TOKEN }}
        run: |
          if (-not $env:CLOUDFLARED_TOKEN) {
            Write-Error "❌ CLOUDFLARED_TOKEN secret not set. Add it in your repository settings."
            exit 1
          }
          Write-Host "Installing Cloudflared service..."
          .\cloudflared.exe service install $env:CLOUDFLARED_TOKEN
          Start-Sleep -Seconds 8
          Write-Host "✅ Tunnel installation attempted. Check your Cloudflare dashboard for the active URL."

      - name: ✅ Done
        shell: pwsh
        run: |
          Write-Host "🎉 Myrtille + Cloudflare tunnel setup complete!"
